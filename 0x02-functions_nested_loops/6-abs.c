#include "main.h"
/**
 * _abs - computes hte absolute value of an integer
 * @r: Character to be printed
 * Return: Always positive r
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
