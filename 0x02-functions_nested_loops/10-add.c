#include "main.h"
/**
 * add - adds two integers and returns the results.
 * @a: first integer
 * @b: second integer
 * Return: The result of the sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
